<Window x:Class="Demo.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Demo"
        xmlns:model="clr-namespace:Demo.Models"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.Resources>
        <model:roleList x:Key="roleList" />
    </Window.Resources>
    <Grid>
        <Button Content="Button" HorizontalAlignment="Left" Margin="604,138,0,0" VerticalAlignment="Top" Width="75" Command="local:MainWindow.enter"/>
        <ComboBox HorizontalAlignment="Left" Margin="271,127,0,0" ItemsSource="{StaticResource roleList}" DisplayMemberPath="Role1" VerticalAlignment="Top" Width="120"/>

    </Grid>
    <Window.CommandBindings>
        <CommandBinding Command="{x:Static local:MainWindow.enter}"
                        Executed="enter_Executed"
                        CanExecute="enter_CanExecute" />
    </Window.CommandBindings>
</Window>
